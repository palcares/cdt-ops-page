# cdt-ops-page

Static ops page. Content is published/removed with `rig page publish|off`
from the main repo. `index.html` is either a placeholder or an encrypted
(StatiCrypt AES-256) document — this repo never contains plaintext content.
